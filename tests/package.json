{
  "name": "fitness-tracker-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:e2e": "vitest run e2e",
    "test:e2e:watch": "vitest e2e",
    "test:api": "vitest run api-connectivity",
    "test:api:full": "run-api-tests.bat",
    "test:e2e:full": "run-e2e.bat",
    "test:coverage": "vitest --coverage",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@vitest/ui": "^2.1.8",
    "cross-spawn": "^7.0.6",
    "node-fetch": "^3.3.2",
    "playwright": "^1.49.1",
    "start-server-and-test": "^2.1.3",
    "vitest": "^2.1.8"
  }
}
